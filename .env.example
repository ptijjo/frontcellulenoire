NEXT_PUBLIC_API_URL=https://api.cellulenoire.fr

# Activer seulement si l'API pose le cookie avec COOKIE_DOMAIN=.cellulenoire.fr
# sinon le middleware redirige en boucle (307) vers /?redirect=/dashboard
COOKIE_MIDDLEWARE=false
